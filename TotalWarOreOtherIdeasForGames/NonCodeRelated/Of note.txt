i dont know what roling back the entityFrameworkCore did to the fuunctionality of the curent models functionality


To Unit test

Finnish The Opperation classes
Add byte image to the rest
Pagination UI  ( next we need to make the get method to send PageModel object, Get method usualy read only the URL query string but i dont think you cant do it)
				( meby the PageModel should be in a ViewModel class)
				(Pagination should have numbers that you can chose from)
				(a way of changing the pageSize)
